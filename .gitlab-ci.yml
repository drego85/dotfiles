image: fedora:30

before_script:
  # Install various commands used during the build.
  - dnf --quiet --assumeyes install ShellCheck findutils make

lint-bash-module:
  stage: test
  script: make lint-bash-module

lint-bash-dotfiles:
  stage: test
  script: make lint-bash-dotfiles

lint-bash-bin:
  stage: test
  script: make lint-bash-bin
